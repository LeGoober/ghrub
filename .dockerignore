node_modules
npm-debug.log
coverage
dist
data
*.db
.git
.github
.env
.env.*
test
# docs/*.md are excluded by the *.md rule; docs/seed/grocery-history.json (the
# seed data the image must ship) is a .json so it stays in the build context.
*.md
